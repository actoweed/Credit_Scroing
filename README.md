# Credit Scoring Project

Для проекта был выбран следующий датасет: https://www.kaggle.com/datasets/julianocosta/home-credit

##  Описание проекта
Этот проект - учебная, но максимально приближённая к реальности реализация **кредитного скоринга**, аналогичного тем, что используются в банках.

Цель модели — оценить **вероятность дефолта клиента (PD, Probability of Default)** и на основе этого значения принять бизнес‑решение:
- одобрить кредит
- отправить на ручную проверку
- отклонить заявку

Проект построен как **ML‑pipeline**, а не просто ноутбук с моделью.

---

##  Бизнес‑логика

В банке модель **НЕ принимает решение напрямую**.

Она выдаёт риск (PD), а дальше применяется **policy‑layer**:

| PD | Решение |
|----|--------|
| PD < 0.30 | approve |
| 0.30 ≤ PD < 0.50 | manual_review |
| PD ≥ 0.50 | reject |

Именно так и реализован инференс в проекте.

---

##  Feature Engineering

Feature engineering вынесен **отдельно от моделинга**, как в реальном проде.

### Что делается:
- медианная импутация числовых признаков
- создание бизнес‑признаков:
  - `CREDIT_INCOME_RATIO`
  - `ANNUITY_INCOME_RATIO`
  - `HAS_CHILDREN`

One‑Hot Encoding **НЕ делается в data pipeline**, чтобы избежать leakage и проблем при инференсе.

---

##  Модели

###  Baseline — Logistic Regression
- использовалась для проверки данных и baseline‑оценки

###  Финальная модель — CatBoostClassifier
Причины выбора:
- нативная работа с категориальными признаками
- устойчивость к выбросам
- хорошая интерпретируемость

---

##  Метрики финальной модели

```
ROC-AUC:   0.76
Accuracy:  0.86
Recall:    0.40
Precision: 0.26
F1-score:  0.31
```

🔍 Основной фокус — **ROC-AUC и Recall**, так как:
- важнее не пропускать дефолты
- допустим больший FP (ручная проверка)

---

##  Инференс

Инференс реализован в `pipelines/inference.py`.

### Как работает:
1. Загружается сохранённая модель
2. Применяется `process_features`
3. Считается PD
4. Применяется policy‑layer
5. Возвращается DataFrame с решениями


---
##  Деплой

Для демонстрации работы модели в условиях, приближенных к реальным банковским процессам, реализован REST API на базе FastAPI.

FastAPI-сервис:
загружает обученную модель CatBoost
принимает данные клиента в формате JSON
применяет тот же feature engineering, что использовался при обучении
возвращает:
вероятность дефолта (PD)
итоговое кредитное решение
Пример логики принятия решения:
```
PD < 0.30 → approve
0.30 ≤ PD < 0.50 → manual_review
PD ≥ 0.50 → reject
```
Таким образом, модель используется не как «чёрный ящик», а как часть decision engine, что соответствует реальным банковским скоринговым системам.
---

##  Что показывает этот проект

- понимание **end‑to‑end ML процесса**
- разделение data / features / modeling / inference
- понимание бизнес‑метрик в скоринге
- работа с PD, threshold и policy
- приближённая к банковской архитектура

---

## Итог

Это не «ноутбук с CatBoost», а **полноценный скоринговый кейс**, близкий к тому, что делают в банках и финтехе.


